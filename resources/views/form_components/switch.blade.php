<div class="mb-3">
    <div class="form-check">
        <input type="checkbox" class="form-check-input" name="{{ $name }}" id="{{ $name }}" @checked($value)>
        <label class="form-check-label" for="{{ $name }}">{{ $translation }}</label>
    </div>
</div>
